# Generated by tools/sync_dependency_groups.py - do not edit.
#
# This is not a real package. It exists so that uv.lock can pin the dependency groups
# that the wheel and sdist builds install, without dragging in the rest of scipy's
# dependency groups. Regenerate it with tools/update_lock.sh.

[project]
name = "scipy-release-deps"
version = "0"
requires-python = ">=3.12"
dependencies = []

[tool.uv]
# a virtual project: there is nothing here to build or install
package = false

[dependency-groups]
build = [
    "meson-python>=0.15.0",
    "Cython>=3.2.0",
    "pybind11>=2.13.2",
    "pythran>=0.18.1",
    "numpy>=2.0.0",
    "ninja>=1.8.2",
]
test-core = [
    "pytest>=8.0.0",
    "hypothesis>=6.30",
    "pooch",
    "pytest-xdist",
    "pytest-timeout",
]
openblas32 = [
    "scipy-openblas32==0.3.34.237.0",
    "pkgconf==2.5.1.post1; sys_platform == \"win32\"",
]
